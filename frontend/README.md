# DroneDream Frontend

React + TypeScript + Vite app shell for the DroneDream MVP. Phase 0 only
establishes routing for the required pages (Dashboard, New Job, Job Detail,
Trial Detail, History) and a minimal layout.

```bash
npm install
npm run dev        # http://localhost:5173
npm run typecheck
npm run lint
npm run build
```
