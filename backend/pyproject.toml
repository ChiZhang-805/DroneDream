[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "drone-dream-backend"
version = "0.1.0"
description = "DroneDream FastAPI backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110,<1.0",
    "uvicorn[standard]>=0.27,<1.0",
    "pydantic>=2.6,<3.0",
    "pydantic-settings>=2.2,<3.0",
    "sqlalchemy>=2.0,<3.0",
    "cryptography>=42.0",
    "PyJWT>=2.10,<3.0",
    "openai>=1.30,<3.0",
    "numpy>=2.0,<2.4",
]

[project.optional-dependencies]
dev = [
    "alembic>=1.13,<2.0",
    "pytest>=8.0",
    "httpx>=0.27",
    "httpx2>=2.0,<3.0",
    "ruff>=0.4",
    "mypy>=1.8",
]
storage = [
    "boto3>=1.34",
]
production = [
    "alembic>=1.13,<2.0",
    "psycopg[binary]>=3.1,<4.0",
    "redis>=5.0,<7.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["UP017"]

[tool.ruff.lint.isort]
known-first-party = ["app", "distribution", "scripts"]
known-third-party = ["alembic", "sqlalchemy"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
