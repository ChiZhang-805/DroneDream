"""Discoverable first-party AUTONOMY plugins.

Every module exposes ``plugin_definition``. The core scans this package instead of
hard-coding plugin identifiers or importing implementations directly.
"""
